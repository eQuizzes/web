const PREFIX = '@EQuiz';

const constants = {
  RESPONSE_QUIZ_STUDENT_ANONYMOUS: `${PREFIX}:responseStudent`,
  TEMPLATE_RESPONSE_QUIZ: `${PREFIX}:templateResponseStudent`,
}

export default constants;
